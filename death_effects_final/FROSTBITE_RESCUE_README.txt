XeroHub · Frostbite Rescue

Frostbite NO se descarta.

Motivo:
- Su Behavior V3 actual tiene Quality V4 bajo, así que no es confiable como timeline principal.
- Pero el snapshot V2 sí contiene el asset del efecto y se conserva como fuente principal.
- El V3 débil queda incluido sólo como referencia; no debe aplicarse automáticamente.

Renderer:
behaviorMode = v2_primary_rescue
required = true

Esto permite incluir Frostbite en el catálogo final sin contaminarlo con un timeline V3 flojo.
